%!TEX TS-program = Arara
% arara: pdflatex: {shell: yes}
% arara: biber
% arara: pdflatex: {shell: yes}

\documentclass[12pt, ngerman, parskip=half, oneside,liststotoc,bibtotoc,BCOR=1cm]{scrbook}

\input{preamble}

%\usepackage[usefilenames,DefaultFeatures={Ligatures=Common}]{plex-otf} %

%\usepackage[urw-garamond]{mathdesign}
\usepackage{fourier}
\setkomafont{disposition}{\normalcolor\bfseries}

% Wie separate Schriftart für Überschriften?

%\usepackage[scaled]{berasans}
%\renewcommand{\familydefault}{\sfdefault} 

\begin{document}
%\input{titelseite}
\maketitle

\frontmatter

\tableofcontents

\listoffigures

\listoftables



\mainmatter

\(a^2 + b^2 = c^2 \)

\include{Kapitel_01}

\include{Kapitel_02}

\backmatter


\printbibliography[title={Bücher},type=book]

\printbibliography[title={Online-Quellen},type=online]


\end{document}